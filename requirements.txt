# NBA Analytics App — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web UI ---
dash==2.17.0
dash-bootstrap-components==1.6.0
plotly==5.22.0                  # Interactive charts rendered inside Dash

# --- NBA Data ---
nba_api==1.11.4                 # Official NBA stats API wrapper (1.11+ adds ScheduleLeagueV2)

# --- Data Processing ---
pandas==2.2.2                   # DataFrames for cleaning and structuring stats
numpy==1.26.4                   # Numerical operations used by scikit-learn

# --- Machine Learning ---
scikit-learn==1.5.0             # Linear regression model for stat prediction

# --- AI Integration ---
openai==1.30.1                  # OpenAI API client for natural language insights

# --- Config ---
pyyaml==6.0.1                   # Reads config.yaml settings file

# --- Environment Variables ---
python-dotenv==1.0.1            # Loads .env file (API keys) safely

# --- Production server (used by Render, Heroku, etc.) ---
gunicorn==22.0.0                # WSGI server that serves `app:server` in prod

# --- Testing ---
pytest==8.2.1                   # Runs unit tests
pytest-mock==3.14.0             # Mocking for unit tests (fake API calls)
